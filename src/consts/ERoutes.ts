enum ERoutes {
    HOME = "/",
    DEMOS = "/demos",
    TOOLS = "/tools",
    LIBS = "/libs",
}

export default ERoutes;

export enum EDemos {
    blank = "/demos/blank",
    GBCS = "/demos/GBCS",
    micecho = "/demos/micecho",
}

export enum ETools {
    ANSI        = "/tools/ANSI",
    ajax2write  = "/tools/ajax2write",
    determinan  = "/tools/determinan",
    forEverLoop = "/tools/foreverloop",
    htmlEditor  = "/tools/htmlEditor",
    htmlEditor2 = "/tools/htmlEditor2",
    mlg         = "/tools/mlg",
    pitung      = "/tools/pitung",
}

export enum ELibs {
    README = "/libs/README.md",
    buried = "/libs/buried.png",
    project = "/libs/project.png",
    ifLine = "/libs/ifLine.js",
    inRange = "/libs/inRange.js",
}
